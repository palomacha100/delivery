<header>
  <div class="header-container">
    <div>
      <%= link_to "Início", root_path, class: 'small-blue-button' %>
    </div>
    <div class="header-inline">
      <p class="text-styled">Logged in as <strong><%= current_user.email %></strong></p>
      <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "small-red-button" %>
    </div>
  </div>
</header>
<main>
  <div class="global-container">
    <h1 class="title-styled">Gerenciamento de pedidos</h1>
    <%= link_to "Novo pedido", orders_new_path, class: 'small-blue-button' %>
  <div>
    <% @orders.each do |order| %>
      <div class="big-column-start-container">
        <h2><%= order.id %></h2>
        <p class="text-styled">Status: <%= order.state %></p> 
        <%= link_to "Ver pedido", order_path(order), class: 'small-green-button' %> 
      </div>
      <hr/>
    <% end %>
  </div>
  </div>
</main>